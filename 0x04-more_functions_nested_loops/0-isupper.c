#include "main.h"

/**
 * _isupper - checks for uppercase character
 *
 * @c: the passed in arguement
 *
 * Result: the returned result
 */

int _isupper(int c)
{
	if (c >= 65 && C <= 91)
		return (1);

	return (0);


}
