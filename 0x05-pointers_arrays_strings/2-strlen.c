#include "main.h"
#include <stdio.h>

/**
 * _strlen - function that returns the length of a string
 *
 * @s: the passed string
 *
 * Return: the returned value
 */

int _strlen(char *s)
{
	_strlen(s);

	return (0);


}
